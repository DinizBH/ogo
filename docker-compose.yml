services:
  db:
    image: postgres:16
    container_name: ogo-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ogo
      POSTGRES_PASSWORD: ogo
      POSTGRES_DB: ogo
    ports:
      - "5432:5432"
    volumes:
      - ogo_db:/var/lib/postgresql/data

volumes:
  ogo_db:
